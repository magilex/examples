apply plugin: 'java'

dependencies {
    compile 'org.apache.commons:commons-lang3:3.4'
}

task quicksort(type:JavaExec) {
    main = 'com.magilex.examples.algorithms.sorting.quicksort.Quicksort'
    classpath = sourceSets.main.runtimeClasspath
}